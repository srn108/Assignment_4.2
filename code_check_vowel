def check_vowel(str1):
    if (char=='a' or char=='e' or char=='i' or char=='o' or char=='u'):
        return True
    else:
        return False
    

char = input('enter a char\n')

check_vowel(char)
